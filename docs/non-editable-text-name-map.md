# Non-Editable Text Name Map

Mapeo semantico inicial para `Micrographics Vol.1 - Fox Rockett Studio/Non-Editable Text/SVG - Non-Editable Text`.

Convencion usada:
- `PascalCase` para el nombre futuro del componente
- nombre basado en la forma visible de la pieza
- cuando la composicion coincide con `Editable Text`, se conserva el mismo nombre semantico para mantener consistencia

## Batch 01

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 1 | `DegradedChannelLockColumn` | Corresponde a la misma columna tecnica que la version editable: estado degradado, canal offline, capa de seguridad bloqueada y ejes de polaridad, pero con el texto ya convertido a trazos. |
| Non-Editable 2 | `DataFragmentArchivePanel` | Mantiene la misma estructura de panel vertical con titulos de archivo, metadata numerica y diagrama orbital central, solo que en version no editable. |
| Non-Editable 3 | `SnapshotVerifyPanel` | Conserva la composicion vertical de snapshot y verificacion con bloques modulares y texto integrado como trazos. |
| Non-Editable 4 | `HardwareCalibrationStatusBanner` | Es la contraparte no editable del banner horizontal de calibracion de hardware, con la misma distribucion de estados y modulos tecnicos. |
| Non-Editable 5 | `DualInitialSystemLockBanner` | Repite la misma pieza horizontal con iniciales grandes, bloque `SYSTEM.DAT / LOCK ENABLED` y estructura orbital central, ahora con texto trazado. |

## Batch 02

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 6 | `DefaultSignalOnlineBanner` | Corresponde al mismo banner horizontal de senal por defecto con iconos de estado a la izquierda y copy tecnico de sistema activo, ahora con texto convertido a trazos. |
| Non-Editable 7 | `MemoryCoreClusterPanel` | Mantiene la misma composicion vertical de `MEMORY CORE`, identificador central y diagrama de nodos enlazados, en version no editable. |
| Non-Editable 8 | `ConfigSensorArrayPanel` | Repite el mismo panel amplio de configuracion con red radial y estructura de sensor array, pero con todo el copy integrado como outlines. |
| Non-Editable 9 | `FusionFirmwareStatusBanner` | Es la contraparte no editable del banner de `FUSION`, con bloques de restore e install y los mismos indicadores tecnicos. |
| Non-Editable 10 | `DeviceLinkSecurityBanner` | Conserva el mismo banner con `DEVICE LINK - ACTIVE`, numero destacado y modulo lateral de seguridad desbloqueada, en texto trazado. |

## Batch 03

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 11 | `InitializeIndexStatusColumn` | Corresponde a la misma columna tecnica con arco lateral, nodos circulares y foco en inicializacion e indice, solo que el texto ya viene trazado. |
| Non-Editable 12 | `AudioSignalVerifiedBanner` | Mantiene la misma composicion lineal de audio y verificacion primaria con iconos en los extremos, en version no editable. |
| Non-Editable 13 | `RelayLinkOfflinePanel` | Repite el mismo panel vertical de enlace caido con estado offline y estructura tecnica interna, ahora con copy convertido a outlines. |
| Non-Editable 14 | `DecipherValidationPanel` | Conserva el mismo panel ancho de `DECIPHER` con validacion, matriz de puntos y bloque de identificacion, en version no editable. |
| Non-Editable 15 | `StandbyWarningStatusBanner` | Es la contraparte no editable del banner de monitoreo con estados `STANDBY`, `WARNING` y `PROCESSING`, manteniendo la misma estructura visual. |

## Batch 04

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 16 | `EpochTransformVerificationPanel` | Corresponde al mismo panel ancho de `EPOCH` con red de nodos y bloques de verificacion y transformacion, solo que el texto ya viene convertido a trazos. |
| Non-Editable 17 | `SubsystemInputStatusPanel` | Mantiene la misma pieza diagramatica con identificador `064`, nodos enlazados y estados de `SUBSYSTEM-01` e `INPUT ARRAY`, en version no editable. |
| Non-Editable 18 | `SecondaryInterfaceChannelPanel` | Repite el mismo panel mixto de interfaz secundaria, canal central y cierre de estado, pero con el copy integrado como outlines. |
| Non-Editable 19 | `DataClusterRemoteInterfacePanel` | Conserva el mismo panel grande de `DATACLUSTER` y `REMOTE INTERFACE - ONLINE`, con la misma franja inferior y estructura general, en version no editable. |
| Non-Editable 20 | `RelayUnitAlertColumn` | Es la contraparte no editable de la columna de `ALERT`, con gran arco lateral, bloque central de relay y acentos de upload y bandwidth. |

## Batch 05

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 21 | `ModuleB7OutputStatusPanel` | Corresponde al mismo panel vertical de `MODULE B7`, badge `OUTPUT` y gran matriz de nodos, ahora con el copy convertido a outlines. |
| Non-Editable 22 | `SecondaryInterfaceOfflineModulePanel` | Mantiene la misma estructura vertical de interfaz secundaria offline con visuales circulares y cierre `[MODULE]`, en version no editable. |
| Non-Editable 23 | `ModuleSyncNodeColumn` | Repite la misma columna tecnica con identificador `0079`, bloque de sincronizacion y secuencia de formas geometricas conectadas, pero con texto trazado. |
| Non-Editable 24 | `MaintenanceModeRadialColumn` | Es la contraparte no editable de la columna radial de mantenimiento con rayos laterales, identificador y nivel de potencia. |
| Non-Editable 25 | `AlternatingCriticalErrorPanel` | Conserva el mismo panel expresivo de `ALTERNATING` y `CRITICAL ERROR`, con su lectura dramatica de sistema rechazado y reboot, en version no editable. |

## Batch 06

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 26 | `ChannelActiveMetricsBanner` | Corresponde al mismo banner horizontal con `CHANNEL 01 - ACTIVE`, metricas de bandwidth y temperatura, solo que el texto ya viene vectorizado. |
| Non-Editable 27 | `NetworkNodeOfflineScalePanel` | Mantiene la misma pieza compacta de nodo offline con escala `1-9` y trama modular, en version no editable. |
| Non-Editable 28 | `ArchiveDefragReloadBanner` | Repite el mismo banner de `ARCHIVE DEFRAG OPTIMIZED` y `PROFILE.INI RELOAD SUCCESS`, con indicadores laterales y copy convertido a outlines. |
| Non-Editable 29 | `EchoDiagnoseRestoreBanner` | Conserva el mismo banner tecnico de `ECHO`, `RESTORE` y `DIAGNOSE`, reforzado por iconografia circular, pero con texto trazado. |
| Non-Editable 30 | `SystemLayerOperationalPanel` | Es la contraparte no editable del panel ancho `SYSTEM LAYER - OPERATIONAL`, con sus modulos inferiores y la misma estructura general. |

## Batch 07

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 31 | `TimeEngineSyncBanner` | Corresponde al mismo banner horizontal de `TIME ENGINE - (SYNCING)` con bloque secundario de `MAP NODE - PROCESSING`, pero con el texto ya convertido a trazos. |
| Non-Editable 32 | `CoreFunctionBootPanel` | Mantiene el mismo panel de estado con `(CORE_FUNCTION)`, `MEMORY CORE - OPTIMIZED` y metricas de boot y CPU, en version no editable. |
| Non-Editable 33 | `MemoryPortUptimeBanner` | Repite el mismo banner lineal de `MEMORY PORT 3 - ACTIVE` y `UPTIME SYSTEM - ACTIVE`, con sus motivos geometricos intactos y el copy vectorizado. |
| Non-Editable 34 | `GatewayTargetCoordinatesPanel` | Conserva el mismo panel tecnico de `813 // GATEWAY`, coordenadas y objetivo de subsystem, ahora con el texto integrado como outlines. |
| Non-Editable 35 | `LocalInterfaceVersionBanner` | Es la contraparte no editable del banner de `LOCAL INTERFACE V6.0.21`, con estado superior de `NETWORK LAYER - ACTIVE` y acentos circulares. |

## Batch 08

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 36 | `ModuleZDiagnosticBanner` | Corresponde al mismo banner horizontal con fecha `NOV / 26`, estado `STABLE` y bloque central de `MODULE Z / SUBROUTINE-04 - PROCESSING`, pero con el texto ya trazado. |
| Non-Editable 37 | `ScanGridPointSignalPanel` | Mantiene el mismo panel circular de escaneo con `GRID POINT - ACTIVE`, `SIGNAL: STRONG` y datos de latencia, en version no editable. |
| Non-Editable 38 | `ActivatedPrimaryRouteBanner` | Repite el mismo banner compacto de `PRIMARY ROUTE - VERIFIED` con sello `[ACTIVATED]` y metadatos tecnicos, ahora con copy convertido a outlines. |
| Non-Editable 39 | `SectorStatusVersionPanel` | Conserva el mismo panel de estado para `SECTOR A` y `SECTOR C`, con matriz de nodos y version al pie, en version no editable. |
| Non-Editable 40 | `RelayOfflinePowerPanel` | Es la contraparte no editable del panel de fallo con `OFFLINE`, `RELAY LINK OFFLINE`, datos de energia y modulo lateral tipo bateria. |

## Batch 09

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 41 | `SystemRebootErrorBanner` | Corresponde al mismo banner horizontal con alerta critica, rotulo central `SYSTEM REBOOT` y nodos A/Z en ambos extremos, pero con el texto ya trazado. |
| Non-Editable 42 | `SubsystemControlNodePanel` | Mantiene el mismo panel vertical con cabecera `NODE`, bloque `SUBSYSTEM-02 UNLOCKED`, metricas y red de nodos de control, en version no editable. |
| Non-Editable 43 | `AnalogVectorStandbyPanel` | Repite el mismo panel tecnico `(ANALOG)` con `STANDBY`, `VECTOR PATH - STABLE` y modulos circulares a la derecha, ahora con copy convertido a outlines. |
| Non-Editable 44 | `EntryPointRestorePanel` | Conserva el mismo panel organico de `ENTRY POINT - ONLINE` y `MODULE Z / RESTORING`, con cluster de pixeles y nodos al costado derecho, en version no editable. |
| Non-Editable 45 | `StaticModuleDisablePanel` | Es la contraparte no editable del panel modular con `STATIC`, `MODULE-D9`, estado `DISABLE` y badge `IDLE`, manteniendo la misma estructura visual. |

## Batch 10

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 46 | `MaintenanceModePowerDial` | Corresponde al mismo dial radial de `MAINTENANCE MODE` con identificador, lectura `POWER: 54%` y etiqueta central `[ PWR ]`, ahora con el texto ya trazado. |
| Non-Editable 47 | `RestartBandwidthProgressDial` | Mantiene el mismo dial circular de `RESTARTING`, `BANDWIDTH: 2.4GBPS` y chips centrales de progreso, en version no editable. |
| Non-Editable 48 | `DiagnosticUnitExportReadyPanel` | Repite el mismo panel ancho con doble cluster circular, bloque `OUTPUT_A1 / EXPORT READY` y cierre de unidad diagnostica, pero con copy convertido a outlines. |
| Non-Editable 49 | `NodeB43OnlineStatusBanner` | Conserva el mismo banner horizontal de `NODE B43`, titular `ONLINE` y lectura de `PACKET LOSS`, en version no editable. |
| Non-Editable 50 | `ChargingPowerStatusBanner` | Es la contraparte no editable del banner minimal de carga con `ACTIVE - ID: 7F-C2 - POWER: 92%` y bloque diagonal al extremo izquierdo. |

## Batch 11

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 51 | `VersionRebuildDoneCornerFrame` | Corresponde a la misma pieza compacta de esquina con marco en L, flecha diagonal y copy vertical de version y rebuild, ahora con el texto ya trazado. |
| Non-Editable 52 | `EchoArchiveRewritePanel` | Mantiene el mismo panel orbital de `ARCHIVE`, `SYSTEM` y `ECHO`, con bloque `CONFIG REWRITE SUCCESSFUL` y cierre `DATA_042`, en version no editable. |
| Non-Editable 53 | `RemoteInterfaceDeviceBanner` | Repite el mismo banner horizontal con titular `DEVICE` y bloque `REMOTE INTERFACE - ONLINE`, pero con el copy convertido a outlines. |
| Non-Editable 54 | `SensorArraySystemBanner` | Conserva el mismo banner espejado con titular `SYSTEM` y bloque `SENSOR ARRAY - PROCESSING`, en version no editable. |
| Non-Editable 55 | `MonitorNodeRunPanel` | Es la contraparte no editable del panel ancho de monitoreo con anillos concentricos y copy `RUN / TARGET: MONITOR NODE / STATE: ACTIVE`. |

## Batch 12

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 56 | `CalibratedSectorValidationPanel` | Corresponde al mismo panel ancho con titular `(CALIBRATED)`, gauge semicircular y estado `VALIDATED`, solo que el texto ya viene trazado. |
| Non-Editable 57 | `ArchiveDecodingAccessPanel` | Mantiene la misma pieza compacta de `ARCHIVE - DECODING // ACCESS GRANTED` con identificador `007`, chip `ORIGIN.E43` y bloque de `RESET`, en version no editable. |
| Non-Editable 58 | `DigitalOutputChannelPanel` | Repite el mismo panel vertical con `2026`, `OUTPUT: DIGITAL`, indice y `CHANNEL: 04`, pero con el copy convertido a outlines. |
| Non-Editable 59 | `ArchiveFrequencyExportBanner` | Conserva el mismo banner horizontal que combina acceso de archivo, bloque `EXPORT//2026` y estado `ALTERNATING FREQUENCY IDENTIFIED`, en version no editable. |
| Non-Editable 60 | `ClassifiedAccessKeyPanel` | Es la contraparte no editable del panel de acceso con `ACCESS: PENDING`, log verificado y cierre `DATA - CLASSIFIED // REROUTING ACCESS KEY`. |

## Batch 13

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 61 | `Port025StandbyLogicBanner` | Corresponde al mismo banner horizontal con estado `OFFLINE : STANDBY // PORT 025`, referencia `DATA.043.XML` y la palabra `LOGIC` armada en nodos circulares, ahora con el texto ya trazado. |
| Non-Editable 62 | `FrequencyOverrideReconnectPanel` | Mantiene el mismo panel panoramico con `FREQUENCY REGISTERED`, sello `OVERRIDE`, etiqueta vertical `STATIC` y bloque de `COMMUNICATIONS RECONNECTING [+]`, en version no editable. |
| Non-Editable 63 | `Variant033DigitalOutputPanel` | Repite el mismo panel compacto con `OUTPUT: DIGITAL`, indice, titular `VARIANT 033`, badge `STABLE` y fila de iconos funcionales, pero con el copy convertido a outlines. |
| Non-Editable 64 | `OverloadedSystemResetPanel` | Conserva el mismo panel de alerta con `SYSTEM RESET [!]`, estado `[OVERLOADED]`, progreso `47/100` e iconos de energia y advertencia, en version no editable. |
| Non-Editable 65 | `AlternatePowerOrbitPanel` | Es la contraparte no editable del panel energetico con titular `ALTERNATE`, lecturas de potencia y voltaje, sello `2026` y anillo orbital de nodos al costado derecho. |

## Batch 14

| Original | Nombre propuesto | Motivo |
| --- | --- | --- |
| Non-Editable 66 | `ActiveDigitalDrivePanel` | Corresponde al mismo panel compacto con sello `ACTIVE`, bloque `OUTPUT: DIGITAL`, indice, `CHANNEL: 04` y estado `DRIVE-STATE: INTACT`, ahora con el texto ya trazado. |
| Non-Editable 67 | `ModuleB43PortStatusStrip` | Mantiene la misma tira horizontal minimal con `MODULE B43`, `PORT: 07`, cluster circular a la izquierda y salida lineal con flecha al extremo derecho, en version no editable. |
| Non-Editable 68 | `SystemYearLatencyPanel` | Repite el mismo panel vertical con arcos laterales, columna de puntos decrecientes, titular `//SYSTEM` y cierre `YEAR 2026`, pero con el copy convertido a outlines. |
| Non-Editable 69 | `ArchiveLogAccessNodePanel` | Conserva el mismo panel ancho con cabecera `ARCHIVE_LOG`, copy `ACCESS / CONFIRMED / NODE: EGH-9320`, rotulo vertical japones y anillo de nodos en la esquina superior derecha, en version no editable. |
| Non-Editable 70 | `InstalledReceiptStatusPanel` | Es la contraparte no editable del panel panoramico con globo orbital, titulares `INSTALLED` y `100%`, y matriz de estados tipo `PRE-PATCH / COMPLETE / CONFIRMING / RECEIPT`. |
